// types.d.ts

interface Point {
    x: float;
    y: float;
} // Point

interface Interval {
    min: float;
    max: float;
} // Interval

type int = number;
type float = number;
type Vec3i = [int, int, int];
type Vec3f = [float, float, float];
